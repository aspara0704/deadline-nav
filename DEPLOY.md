# v0.7.4 の確認・配布

1. PRの `Verify local IC release` が成功したことを確認する。
2. マージ・公開後、Safariで再読み込みし、フッターが `v0.7.4` であることを確認する。
3. 設定 → 開発・診断で「全国ICカタログ: 2,085件（アプリ同梱）」を確認する。
4. v0.7.3で使った条件に近い条件で通常ケースを計算し、IC候補が維持されることを確認する。
5. **今回の計算**で `NAVITIME IC: 0 req`、`Overpass: 0 req`、Route Matrix 99 elements以下を確認する。
6. 通常ケースでは「通常: … 評価済除外 0 …」になることを確認する。
7. 期限を厳しくするなどして回復探索が発生するケースを1回作り、「回復: … 評価済除外 N → Matrix新規 M …」で N > 0 になることを確認する。回復段階のMatrix新規候補に通常段階の12件が再送されないことがv0.7.4の主要な合格条件。
8. 必要ならさらに厳しい条件で最終fallbackも同様に確認する。ただし実機で自然に発生しなければ、自動テスト通過をもって必須とはしない。

v0.7.4では料金取得と走行中の軽量更新には手を入れない。

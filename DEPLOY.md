# v0.7.3 の確認・配布

1. PRの `Verify local IC release` が成功したことを確認する。
2. Actionsのartifact、または `python tools/package-release.py` で生成したZIPを利用する。
3. マージ・公開後、Safariでアプリを再読み込みし、フッターが `v0.7.3` であることを確認する。
4. 設定 → 開発・診断で「全国ICカタログ: 2,085件（アプリ同梱）」を確認する。
5. 京都→大阪市都島区など、v0.7.2で確認した条件に近い条件で計算する。
6. **今回の計算**について、`NAVITIME IC: 0 req` と `Overpass: 0 req` を維持していることを確認する。
7. Route Matrixが **99 elements以下**であることを確認する。通常ケースでは30〜40台を目安にするが、候補の正しさを優先する。
8. 「候補絞り込み」に、通常・回復・最終の各段階で `発見 → Matrix送信 → 概算safe → 精査safe` が表示されることを確認する。
9. v0.7.2で有効だったIC候補が、不自然に消えていないことを確認する。

v0.7.3では、料金取得、同一計算内の再評価廃止、走行中の軽量更新には手を入れない。これらは後続版で扱う。

# v0.7.5 の確認・配布

1. PRの `Verify local IC release` が成功したことを確認する。
2. マージ・公開後、Safariで再読み込みし、フッターが `v0.7.5` であることを確認する。
3. 通常計算で高速入口候補を1件確定する。
4. 設定 → 開発・診断の「走行中の軽量更新をテスト」を1回押す。
5. **今回の計算**が原則 `Google Routes Pro: 2 req`、`Route Matrix Pro: 0 elements / 0 req` になり、「走行中更新: 軽量確認成功: ...（全探索省略）」と表示されることを確認する。
6. IC候補・高速切替期限・目的地到着予想が引き続き妥当であることを確認する。
7. `NAVITIME IC: 0 req`、`Overpass: 0 req` を確認する。
8. 可能なら到着期限を厳しくして推奨ICが無効になる条件を作り、軽量確認後にフル探索へフォールバックすることも確認する。この確認は補助であり、通常の軽量成功経路を主要合格条件とする。

v0.7.5では料金ローカル化など後続テーマには手を入れない。

# v0.7.2 修正版の確認・配布

1. PRの `Verify local IC release` が成功したことを確認する。
2. Actionsの `deadline-nav-local-ic` artifact、または `python tools/package-release.py` で生成したZIPを利用する。全国ICデータは生成済みで同梱される。
3. マージ・公開後、Safariでアプリを再読み込みする。`index.html` のアプリスクリプトとICデータURLは修正版用のキャッシュ識別子を持つ。
4. 新規ブラウザプロファイル等で、旧キャッシュのない状態から開く。
5. 設定 → 開発・診断で「全国ICカタログ: 2,085件（アプリ同梱）」を確認する。
6. 京都から大阪市都島区などの固定条件で計算し、**今回の計算**の `NAVITIME IC: 0 req` と `Overpass: 0 req` を確認する。月間値には旧版の通信が残る場合がある。
7. 巨椋池ICがローカルの候補に存在することは自動テストで確認済み。実際に推奨されるかは経路・時刻・期限次第なので、常に推奨されることを合格条件にしない。
8. 巨椋池ICを使うGoogle経路が正しい入口・方向を通ることを実機で確認する。代表点の追加だけではこの確認は完了しない。

Matrix探索・料金ロジックは変更していない。実APIによる診断と実機確認が済むまで、この修正版のリリース確認は未完了とする。
